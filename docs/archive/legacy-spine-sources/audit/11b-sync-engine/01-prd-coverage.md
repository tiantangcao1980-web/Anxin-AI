# TASK-11b Sync Engine — PRD Coverage

| Requirement | Current Status | Evidence |
|---|---|---|
| P0-1 desktop offline queue schema | Unsigned packaged/runtime evidence closed; signed evidence open | `desktop/migrations/001_offline_queue.sql` defines `offline_tasks`, `sync_log`, `local_artifacts`, local business tables, settings, and indexes; `desktop/src/services/local_db.rs::sqlite_migrations()` exposes the schema contract for `sqlcipher:anxin_local.db`; `desktop/src/services/secure_db.rs` applies it through Rust-owned SQLCipher; `scripts/desktop-runtime-smoke.sh`, `scripts/desktop-installed-profile-smoke.sh`, `scripts/desktop-release-profile-smoke.sh`, and `scripts/desktop-release-runtime-smoke.sh` cover local/debug/unsigned release migration paths; signed/notarized packaged-profile migration proof still missing |
| P0-2 desktop push reads pending SQLite rows | Closed for code path and unsigned packaged loopback; shared-staging/signed evidence open | `frontend/src/lib/api-adapter.ts::runLocalSyncWithDependencies` / `triggerLocalSync` read pending/failed `sync_log` rows and post `/sync/push`; `api-adapter.sync.test.ts` runs an injected SQLite + mocked backend push cycle; `desktop/src/services/sync_engine.rs` and `desktop-release-runtime-unsigned-smoke-20260509.json` prove the unsigned release `.app` binary pushes `2` retryable records to a loopback HTTP backend with bearer auth |
| P0-3 desktop pull writes local SQLite tables | Closed for code path and unsigned packaged loopback; shared-staging/signed evidence open | `runLocalSyncWithDependencies` / `triggerLocalSync` pull `/sync/pull`, write messages/documents/conversations/cases/contracts/settings/artifacts locally, and advance `sync.last_server_version`; injected SQLite + mocked backend pull test verifies local writeback; `--sync-loopback-smoke` writes `1` remote document and advances cursor to `13` from the unsigned release `.app` binary |
| P0-4 conflict resolver and conflicts table | Partial | conflict rows are persisted as `sync_log.status='conflict'`; `/sync-conflicts` provides keep-local/keep-remote/merge editing; unsigned packaged loopback marks `1` conflict row; real interactive Tauri UI conflict-resolution smoke still missing |
| P0-5 backend device/user/version incremental sync | Backend closed; shared-staging transcript open | `backend/src/models/sync.py`, `backend/src/services/sync_service.py`, `backend/alembic/versions/038_add_sync_log.py`, `tests/test_sync_log_service.py`; local unsigned loopback proves the desktop binary can speak the sync contract, but a shared pre-production backend transcript is still missing |
| P0-6 retry loop | Closed for code path and unsigned packaged loopback; shared-staging/human UI evidence open | failed rows write `retry_count`, `next_retry_at`, and `needs_human`; injected SQLite test verifies failed push rows are deferred for retry; `--sync-code-smoke` and `--sync-loopback-smoke` prove retry-to-human handling in the unsigned release packaged binary; full packaged UI human-intervention transcript still missing |
| P0-7 sqlcipher/keyring | Unsigned packaged-profile closed; signed/notarized proof open | Rust-owned SQLCipher/keyring path is implemented in `desktop/src/services/secure_db.rs`; `getDesktopSQLiteSecurityStatus()` reports encrypted/keyring-backed/non-release-blocking; `scripts/desktop-sqlite-security-gate.sh` passes; `scripts/desktop-installed-profile-smoke.sh` and `scripts/desktop-release-profile-smoke.sh` prove plaintext migration, keyring round-trip, second-launch reopen, ordinary sqlite3 plaintext-read rejection, and unsigned release profile/performance behavior |

## Product Impact

- Cross-device continuation is still blocked by signed/notarized package evidence, shared-staging backend sync transcript, interactive conflict/human-intervention UI proof, and mobile/uni-app continuation verification.
- Backend now has a durable sync target, and desktop has a Rust SQLCipher path with injected tests plus unsigned packaged-binary push/pull/conflict/retry loopback evidence instead of an empty push/pull command.
