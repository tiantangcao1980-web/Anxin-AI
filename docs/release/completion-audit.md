# 商业交付目标完成度审计

> 日期：2026-05-08
> 当前结论：目标尚未完成，不能调用完成状态。项目已有强代码级基线，但真实商业交付证据仍存在阻断项。

## 1. 目标拆解

用户目标可拆为以下可验收交付物：

1. 使用 CLI/MCP/源码检索对本仓库进行代码级扫描，确保后续开发基于准确事实；GitNexus 只能作为辅助索引。
2. 完成 GitNexus 最终发布索引卫生配置，但不把 embeddings 排障作为当前开发主线。
3. 在代码级分析之后，使用 OpenSpec 或 goal-prompt-builder 建立项目规范和验收标准。
4. 基于规范与 `/goal` 目标，从当前现状推进到商业交付：开发、测试、发布证据、回滚与风险清单完整。
5. 在宣称完成前，逐项核验文件、命令、测试、门禁和发布证据，不接受代理信号作为充分证明。
6. 2026-05-08 新增定位：把项目规范升级为中小企业双边平台 + 全设备智能助手，覆盖桌面主工作站、移动远控、任意 LLM/Skills/MCP、独立知识库和本地模型安全。

## 2. Prompt-to-Artifact Checklist

| 要求 | 证据 | 当前状态 | 缺口 |
|---|---|---|---|
| CLI/MCP 代码级扫描 | `.gitnexus/meta.json` 当前记录非零 embeddings 且 `capabilities.vectorSearch.status=vector-index`，但 direct GitNexus CLI 当前无法解析本仓 repo/cypher，且索引落后于最新本地提交；direct binary 的 `cypher/status/detect-changes` 仍是最终 release 前必须复跑的 commit-scoped 证据；历史 `detect-changes --scope all` 在大 dirty snapshot 上显示 `380 files / 6037 symbols / 240 affected processes / critical`；MCP `list_repos` 曾可读并返回本仓 embeddings，但本次 Codex MCP 会话的 `query/detect_changes` 返回 `Transport closed`；`scripts/gitnexus-index.sh` 现在会显式报告 dirty worktree drift | GitNexus 当前只能作为过期历史上下文；最新本地提交未完成 GitNexus 复验 | `npx gitnexus@rc` wrapper 仍需规避；direct CLI repo 解析/cypher 需复跑修复；最终 release 前仍需重跑 GitNexus，并继续用 `rg`、源码阅读和测试补盲 |
| 优化 GitNexus 索引范围 | `.gitnexusignore` 排除本地状态、缓存、密钥、构建产物；`.gitignore` 排除 `.gitnexus.*` | 已完成 | 后续新增生成目录仍需维护 ignore |
| GitNexus 可复跑入口 | `scripts/gitnexus-index.sh` 默认结构重建，`--embeddings` 显式启用并先跑 tiny embedding/vector-index preflight；支持 `GITNEXUS_BIN`、`GITNEXUS_NPX_SPEC=gitnexus@rc` 和 `GITNEXUS_REPO_NAME`；preflight 通过后才备份 `.gitnexus` 并写主仓；embedding 模式会跑 `cypher` 真实计数；context smoke 和 repo-scoped `query --repo Anxin-Smart-Legal-Services` smoke 可防止 multi-repo 查询歧义 | 已完成脚本入口 | `npx gitnexus@rc` wrapper 仍需规避 |
| 商业发布门禁 | `scripts/commercial-readiness-gate.sh` 检查 release artifacts、`docs/release/commercial-delivery-checklist.json` prompt-to-artifact 清单引用、`docs/release/commercial-delivery-lanes.json` 并行 lane 写入范围/外部输入/完成门、支付/电签 live runner 必需 env 模板字段、最终 GitNexus 索引卫生、dirty worktree release-blocking gate、外部证据 `Status: complete`、complete evidence 的 Owner/Environment/Date range 元数据、Required Scope 未闭合行、空 artifact reference、仓库内 artifact 路径存在性和正文 pending/not-ready 冲突、artifact JSON 基础 schema/脱敏边界、RAG full50 provenance、RAG full50 failure diagnostic schema、移动设备手工证据模板 schema、移动/小程序 code smoke schema、iOS Simulator Expo Go 支持性 smoke schema、桌面 installed-profile smoke schema、桌面 runtime code smoke schema、桌面 release runtime unsigned smoke schema、桌面 release packaged-profile smoke schema、桌面 release package dry-run、桌面 release preflight schema、支付/电签 sandbox preflight/live schema、artifact validator warnings 转发、dirty worktree 分类盘点、发布证据 secret/PII 扫描、桌面加密 gate、ruff/mypy 基线；`--with-local-tests` 现在也包含 workstation settings Playwright e2e、Agent 治理工作台 Playwright e2e、agent governance、capability route、agent governance model/service、agent approval service/API、MCP route governance、approval authorization 和 skill governance gate tests | 已完成失败型 gate；`commercial-delivery-checklist.json` 已把 10 个成功标准映射到 artifact/verification/current result；`commercial-delivery-lanes.json` 已把 8 条并行 lane 映射到 evidence/write_scope/external_inputs/completion_gate，并由 `backend/tests/test_commercial_delivery_lanes.py` `3 passed` 覆盖；`scripts/validate-release-evidence.py` 已加防线拒绝“顶部 complete 但元数据仍 TBD、表格仍 pending/TBD/缺失、artifact reference 为空、仓库内 artifact 路径不存在或正文仍声明 pending/not-ready”的假闭环，支持 `--json` 机器可读摘要，并由 `backend/tests/test_release_evidence_validation.py` `9 passed` 覆盖；`scripts/validate-release-artifacts.py` 会拒绝坏 JSON、缺 `generated_at`/`mode`、未脱敏敏感值、缺失/非商业 RAG full50 provenance、不完整 RAG full50 failure diagnostic、不完整移动设备手工模板、不完整移动/小程序 code smoke、不完整 iOS Simulator Expo Go 支持性 smoke、不完整桌面 installed-profile smoke、不完整桌面 runtime code smoke、不完整桌面 release runtime unsigned smoke、不完整桌面 release packaged-profile smoke、不完整桌面 release package dry-run、不完整桌面 release preflight、不完整支付/电签 sandbox preflight，以及带 skipped/fail 或缺运行时前置物的支付/电签 live artifact，并由 `backend/tests/test_release_artifact_validation.py` `30 passed` 覆盖；`scripts/commercial-readiness-gate.sh` 会把 artifact validator 的 `WARN:` 输出转为 gate warning，并由 `backend/tests/test_commercial_readiness_gate.py` 覆盖；`scripts/release-worktree-inventory.py` 会把 dirty worktree 分为 release_delivery/generated_or_runtime/local_secret/unknown，并由 `backend/tests/test_release_worktree_inventory.py` `2 passed` 覆盖；`backend/tests/test_agent_governance_models.py` 已加入商业本地门禁，锁住 Agent 控制面模型/迁移不保存真实密钥/原始 token；`backend/tests/test_agent_governance_service.py` 已加入商业本地门禁，锁住 DB-backed route-token lease hash-only、跨实例验证、组织隔离、consumer mismatch、过期和撤销后下一次调用失败；`backend/tests/test_agent_approval_service.py` 已加入商业本地门禁，锁住高风险审批创建、payload 脱敏、授权角色审批、过期/撤销 fail-closed、action/route mismatch、workspace-control runtime_not_integrated fail-closed 和审计写入；`backend/tests/test_agent_approval_api.py` 已加入商业本地门禁，锁住正式审批 API 的未登录拒绝、scope、员工自批拒绝、管理员 approve/revoke、validate、audit-events、audit-export、workspace-control runtime_not_integrated fail-closed 和 payload 脱敏；`frontend/e2e/agent-approval-workspace.spec.ts` 已加入商业本地门禁，锁住桌面端进入审批工作台、执行批准动作、审批审计时间线、审批审计 JSON 导出、Skill 治理创建/评测/批准/灰度/审计导出、已批准行控制动作拒绝和移动端视口不横向溢出；`backend/tests/test_mcp_route_governance.py` 已加入商业本地门禁，锁住 MCP tool execution 缺 route token fail-closed、DB route token 放行、route revoke 后拒绝和 consumer mismatch；`backend/tests/test_commercial_action_audit.py` 已加入 `--with-local-tests` 的支付/电签切片，锁住关键商业动作审计；`backend/tests/test_business_authorization_guards.py -k 'approval or template'` 已加入商业本地门禁，锁住审批批量越权、列表越权、模板写入/使用越权、过期审批和 admin 跨组织越权；`backend/tests/test_skill_evolution_service.py`、`backend/tests/test_skill_service.py`、`backend/tests/test_skill_governance_models.py`、`backend/tests/test_skill_governance_service.py` 与 `backend/tests/test_skill_governance_api.py` 已加入商业本地门禁，锁住 Skills 自我进化不能绕过评测/审批/回滚，也锁住 DB-backed enabled version、组织隔离、API scope、API audit export、append-only audit 和迁移边界 | 当前应失败，直到所有阻断项闭合且交付文件提交后完成最终 GitNexus 复验；GitNexus embedding 不再作为当前开发主线排障项 |
| 静态质量基线采集 | `scripts/static-quality-baseline.sh` 可采集 diff、ruff、mypy、frontend、desktop、release evidence secret/PII scan、full-repo secret/mock scan 的退出码和数量摘要；当前采集为 ruff `0`、mypy `0`、mypy zero-baseline gate `0`、release evidence secret/PII scan `0`、secret scan `0`、mock/fallback scan `0`；全仓 Ruff 与 mypy 均已清零 | 已完成采集入口并完成多轮低风险收敛；`scripts/mypy-baseline-check.sh` 默认 ceiling 已降为 `0`；`static-quality-baseline.md` 已记录 zero-baseline 规则 | 后续必须保持 mypy 零回退 |
| 外部证据模板 | `docs/release/evidence/payment-sandbox.md`、`esign-sandbox.md`、`desktop-runtime-smoke.md`、`rag-full50-live-baseline.md`、`mobile-device-smoke.md`、`static-quality-baseline.md`；`docs/release/evidence-collection-runbook.md` 已把支付、电签、桌面、RAG 和移动/小程序拆成可并行采集清单；`docs/release/external-inputs-checklist.md` 已集中列出外部账号、证书、设备、语料和 release 包输入；`docs/release/external-resource-handoff.md` 已给出外部资源到位后的字段、命令和 completion evidence；`.env.example` 与 `backend/.env.example` 已显式列出支付/电签 live runner 必要字段；`scripts/sandbox-evidence-runner.py` 已提供支付/电签配置预检和可选 live 采集入口；`scripts/desktop-release-preflight.sh` 已提供桌面 signed/notarized release 前置条件预检；`docs/release/evidence/artifacts/payment-sandbox-preflight-20260507.json`、`esign-sandbox-preflight-20260507.json`、`desktop-release-preflight-20260507.json`、`desktop-release-preflight-unsigned-20260507.json` 和 `desktop-release-unsigned-local-build-20260507.json` 已生成脱敏配置/支持性预检；`scripts/release-evidence-secret-scan.sh` 已对证据目录做明文密钥/PII 扫描 | 模板已完成；并行采集 runbook 已完成；外部输入清单与执行交接单已完成；env 模板字段对齐已完成；static quality baseline 已完成；RAG 内建 full50 已完成；支付/电签采集入口、桌面 release preflight、unsigned release app-only 支持性证据和配置预检已完成；证据脱敏 gate 已完成 | 支付、电签、桌面 runtime、移动/小程序仍为 `Status: pending` |
| embeddings 配置 | `.env.example` 新增 `GITNEXUS_EMBEDDING_URL/MODEL/DIMS/API_KEY` 示例；脚本校验 base `/v1` URL 和 dims，并可从 `.env`/`backend/.env` 自动复用 `EMBEDDING_*`；endpoint probe 与 GitNexus HTTP embedder 均可返回 1024 维向量；direct rc binary 曾生成主仓非零 embeddings 并通过 cypher 计数，但当前 direct CLI repo 解析/cypher 复验失败，且最新本地提交尚未完成重建 | embedding 路径曾可用但当前索引不是最新提交证据 | 语义结果仍不能替代源码/测试证据；最终 release 前需重跑 |
| 新增符号入图验证 | direct rc binary 可用 `context runLocalSyncWithDependencies`、`context getDesktopSQLiteSecurityStatus`、`context sqlite_migrations` 定位；repo-scoped `query` 可返回支付 webhook/退款幂等、桌面 SQLite 安全、移动 refresh-token/auth 相关定义；`detect-changes --scope all` 已确认当前未提交交付面为 critical 级影响 | 索引 metadata、embeddings 与读取型 CLI 已验证 | 新增符号覆盖必须继续用 `rg`、源码阅读和测试补盲；提交后必须重跑索引 |
| OpenSpec 规范 | `docs/openspec/01-commercial-delivery-spec.md`、`docs/openspec/00-intelligent-assistant-platform-spec.md` | 已完成当前定位版 | 规范需随后续外部证据更新；桌面配置面、移动远控和扩展能力仍需实现证据 |
| 测试规范 | `docs/openspec/02-commercial-delivery-test-spec.md` | 已完成当前定位版 | Ruff 已清零；backend mypy zero-baseline gate 已机器化；新增全设备智能助手验收仍待代码/真机证据 |
| 审计任务文档 | `docs/audit/PLAN.md`、`docs/audit/SUMMARY.md`、各模块 `docs/audit/<NN-module>/` | 已建立并持续更新 | 后续阻断项需继续补 `00..05.md` 证据 |
| 发布就绪判定 | `docs/release/commercial-delivery-readiness.md` 明确 Not ready | 已完成真实判定 | 商业发布仍阻断 |
| 测试证据清单 | `docs/release/test-evidence.md` | 已完成当前证据汇总 | 真实沙箱/真机/预发证据不足 |
| 回滚手册 | `docs/release/rollback-runbook.md` | 已建立 | 仍需真实演练日志 |
| 安全隐私清单 | `docs/release/security-and-privacy-checklist.md` | 已建立 | SQLCipher/keyring 代码级门禁和本地 installed-profile reopen/migration smoke 已通过；移动 Expo audit 已清零；仍缺 signed packaged UI/profile migration evidence 和生产密钥轮换演练 |
| 后端默认回归 | `cd backend && ./.venv/bin/pytest -q` -> `736 passed, 1 skipped, 12 warnings` | 代码级通过 | 不覆盖真实支付/电签/真机 |
| 前端质量门禁 | `npm run lint`、`npm test`、`npm run build` 通过；最新完整本地门禁中 Vitest 为 `12 files / 45 tests passed`；`npm audit --omit=dev --registry=https://registry.npmjs.org` 返回 `found 0 vulnerabilities`，并写入 `docs/release/evidence/artifacts/frontend-npm-audit-prod-20260507.json` | 代码级和生产依赖审计通过 | Vite chunk/lottie warning 仍需发布前评估 |
| 桌面代码门禁 | 既有 `desktop-runtime-code-smoke-20260507.json` / `desktop-runtime-ui-smoke-20260507.log` 记录过 unsigned debug `.app` self-test/runtime/WebView page-load 代码级通过；fresh local 复跑当前在 debug `.app` runtime startup 的 AppKit registration 前 `Abort trap: 6`，因此不能把 debug bundle runtime 当成当前稳定 passing evidence；Rust IPC 离线任务/同步状态已改为真实读写本地队列与 `sync_log`，不再伪造空 payload 成功；`bash scripts/desktop-installed-profile-smoke.sh --out docs/release/evidence/artifacts/desktop-installed-profile-smoke-20260507.json` 通过本地 installed-profile keyring/reopen/明文迁移/普通 sqlite3 拒读；`scripts/desktop-sqlite-security-gate.sh` 通过，确认 Rust SQLCipher/keyring、frontend package removal、Tauri capability removal 和 self-test contract 对齐；`scripts/desktop-release-package.sh --dry-run` 写出 `desktop-release-package-dry-run-20260507.json` 并记录 `hdiutil` / DiskManagement probe pass；`scripts/desktop-release-preflight.sh` 写出 `desktop-release-preflight-20260507.json`；`cd desktop && cargo tauri build --ci --bundles app,dmg --no-sign` 已在 unsandboxed macOS 环境产出 unsigned release `.app` 与 DMG；`scripts/desktop-release-runtime-smoke.sh` 写出 `desktop-release-runtime-unsigned-smoke-20260507.json` 和 UI log，证明 unsigned release packaged self-test/startup/WebView page-load 通过；`scripts/desktop-release-profile-smoke.sh` 写出 `desktop-release-profile-unsigned-smoke-20260508.json`，证明 unsigned release `.app` 二进制可完成 plaintext-to-SQLCipher migration、真实 keyring 创建、无显式 key 复开、普通 `sqlite3` 拒读和 SQLCipher 100/500 performance P95 `3.5ms` / `1.8ms`；`desktop-release-preflight-unsigned-20260507.json` 确认 release `.app` 和 DMG exist，但 blockers 仍为签名、公证、codesign verify 和 signature authority | 代码级、SQLCipher/keyring 安全门禁、本地 installed-profile reopen/migration artifact、migration SQL、SQLite 性能基线、production APNs entitlement、桌面 release package dry-run、release preflight、unsigned release app+DMG build、unsigned release packaged runtime smoke 和 unsigned release packaged-profile/performance smoke 通过 | 缺 signed/notarized installer、signed packaged-profile migration transcript、signed packaged runtime 性能基线和跨设备连续会话 |
| 小程序/移动门禁 | `bash scripts/mobile-device-smoke.sh --out docs/release/evidence/artifacts/mobile-mini-code-smoke-20260508.json --manual-template-out docs/release/evidence/artifacts/mobile-device-manual-template-20260508.json` 通过：移动端 `8 files / 27 tests passed`、tsc 通过；safe-probe hardening 后独立 `cd mobile && npm test` 为 `8 files / 32 tests passed`，`npm run typecheck` 通过；mobile result surface guard 通过，确认尽调/知识库提交后使用页面内结果摘要卡而非 Alert-only 反馈；mobile lawyer conversion guard 通过，确认找律师移动端保留真实咨询创建、AI 匿名摘要、律师选择、匿名咨询室和委托 API 路径，且不跳转不存在的律师详情页；Expo config/SDK/Metro config guard 通过，`npx expo-doctor` `17/17 checks passed`；移动 production `npm audit --omit=dev` `0`；小程序 tsc/build 通过；mini-program privacy boundary guard、mini-program navigation boundary guard、WeChat DevTools CLI project smoke、refresh auth guard、mobile/mini privacy network guard、fake fallback guard 和 mini-program design token guard 通过，并写出代码级 artifact 和手工设备证据模板；`mobile-device-host-probe-20260508.json` 确认 iOS Simulator 可用、ADB 无连接设备、Android emulator CLI 缺失、WeChat DevTools 与 WeChat app 存在；`mobile-ios-simulator-expo-go-smoke-20260508.json` 证明 iPhone 17 Simulator + Expo Go 可完成本地 iOS bundle，但 `release_evidence_complete=false`；移动端弱网 refresh-token 不误清会话已有单测；移动请求层已透传 `X-Privacy-Mode`，且 local 模式在 fetch 前 fail-closed；移动桌面控制页已接入 `/sync/remote-control/status`，local 模式不发网络请求，confirmed pairing 下可申请短期 `desktop:control` route token 并只入队 `desktop.status_probe`；小程序请求层已透传 `X-Privacy-Mode`，local/top-secret 模式在 `Taro.request` 前 fail-closed，登录入口在 `Taro.login` 前阻断；小程序首页/个人中心主入口不再有空路径或“功能开发中”死胡同，统一跳转 AI 助手并预填业务问题；小程序 refresh-token 已区分认证失效和临时刷新失败；messages/tasks 详情页已移除静默假数据兜底；`getDetailLoadErrorMessage` 覆盖字符串 transport error 和 status 优先级；移动/小程序最小触控 token 底座已补；小程序语义 token 层已补并迁移首页/聊天/个人中心与 `app.config.ts`；移动 production npm audit 已通过定向 overrides 清零，并写入 `mobile_npm_audit.status=passed,total=0` artifact 字段由 validator 锁住 | 本地代码级与 iOS Simulator 支持性 app-run 通过 | 缺真实/官方 iOS 设备故事、Android、交互式微信开发者工具完整验收、移动远控跨设备 host 回传和跨设备连续会话证据 |
| 支付商业证据 | provider/client/webhook 代码级测试；`scripts/sandbox-evidence-runner.py` 预检/live 采集入口；预检 artifact 已新增 `runtime_prerequisites` 和 `external_evidence_requirements`，提前暴露退款/查单/关单订单 ID、官方回调、重复幂等、失败重试和平台 key/cert 轮换等 live 前置证据；live validator 会把退款 `pending`、关单/取消 `False` 标为 `pending`，避免误报 `pass`；`backend/tests/test_sandbox_evidence_runner.py` `7 passed` 覆盖 live 确认门、脱敏写出、env 模板字段同步、runtime prerequisites、外部证据槽、模板 live 参数、handoff/runbook/48h 计划参数防漂移和 honest-gating validators；支付/电签相关 provider/webhook/退款幂等/关键动作审计测试本轮 gate 切片为 `62 passed`；2026-05-07 配置预检 artifact 已重写 | 代码级部分完成；配置预检完成 | 微信/支付宝真实沙箱、证书轮换、退款/关闭/重试证据缺失；当前还缺 WeChat Pay/Alipay app/merchant/key/callback/webhook 配置和已支付/待关闭沙箱订单 ID |
| 电签商业证据 | e签宝/法大大 provider 与 webhook 代码级测试；`scripts/sandbox-evidence-runner.py` 预检/live 采集入口；预检 artifact 已新增 `runtime_prerequisites` 和 `external_evidence_requirements`，提前暴露测试合同 URL/file ID、completed flow ID、官方回调、重复幂等和失败重试等 live 前置证据；live validator 会把无 signer URL 标为 `pending`、空签署文件下载标为 `fail`；`backend/tests/test_sandbox_evidence_runner.py` `7 passed` 覆盖 live 确认门、脱敏写出、env 模板字段同步、runtime prerequisites、外部证据槽、模板 live 参数、handoff/runbook/48h 计划参数防漂移和 honest-gating validators；支付/电签相关 provider/webhook/退款幂等/关键动作审计测试本轮 gate 切片为 `62 passed`；2026-05-07 配置预检 artifact 已重写 | 代码级部分完成；配置预检完成 | 真实商户沙箱、签署/撤销/下载/事件清单缺失；当前还缺 e签宝/法大大 app ID、app secret、官方 webhook enable 配置、测试合同文件和已完成签署 flow ID |
| RAG 商业证据 | live Qdrant smoke、source link、PII/RBAC、Playwright 引用点击；`eval/export_builtin_legal_full50.py` 已从内建法条库导出 `42` 个法条 chunk 与 `50` 条非 smoke golden；`eval/rag_live_qdrant_full50.py` 已跑通 full50 preflight、live Qdrant collection `rag_eval_full50_builtin_20260507` 和 `50` 条 predictions；`eval/rag_quality.py` 已写出 full metrics，recall@10 `1.000`、MRR `1.000`、NDCG@10 `0.997`；`scripts/validate-release-artifacts.py` 与 `scripts/validate-release-evidence.py` 已验证 RAG artifacts/evidence；旧 `eval/rag_golden_set.jsonl` 与 smoke corpus 仍作为负向诊断保留 | 内建 RAG full50 证据完成 | 外部客户知识库评测可作为上线后质量扩展，不再作为当前内建 RAG 阻断 |
| 案件/市场商业证据 | 状态机、owner/assignee/admin、冲突阻断、公平轮询测试 | 代码级核心完成 | 律所 RBAC 全矩阵、案源市场 8 API、Playwright 三角联通缺失 |
| 静态质量门禁 | 全仓 Ruff 清零；全仓 backend mypy 清零；前端 lint 通过；前端生产依赖 audit 清零 | 分层达标 | 前端/桌面构建 warning 仍需发布前复核 |

## 3. 当前已完成的高价值内容

- GitNexus `.gitnexus/meta.json` 有非零 embeddings，但当前 direct rc binary 无法解析本仓 repo/cypher，且索引落后于最新本地提交；direct rc binary 只能作为后续复验入口，不能把当前索引当作最新提交放行证据。
- OpenSpec、测试规范、审计计划、发布就绪、测试证据、回滚和安全隐私文档已经形成一套可追踪交付面。
- 商业发布 gate 已建立，可机器化阻止缺少沙箱/真机/runtime/质量基线证据、GitNexus indexed/current commit 不一致或发布证据含明文密钥/PII 的误发布。
- 后端、前端、桌面、小程序、移动端都有当前代码级测试或构建证据。
- 多个 P0 业务链路已由 mock/UI 骨架推进到真实服务层或协议层代码：认证、模式守卫、对象存储、合同生命周期、支付/电签 webhook、订阅、IM、案件、律师市场、尽调、RAG 与桌面同步底座。2026-05-08 已把“律师市场”规范上升级为“专业服务市场”，并把 LLM/MCP/Skills/knowledge/local LLM 基础纳入全设备智能助手验收口径。

## 3.1 本轮完成审计实测

| 检查 | 实测结果 | 结论 |
|---|---|---|
| `.gitnexus/meta.json` | 当前 meta 有非零 embeddings 和 `vectorSearch.status=vector-index`，但 indexed commit 落后于最新本地提交，且 direct CLI cypher/repo 解析未通过 | GitNexus 仅作历史上下文；最新提交仍需重建/复验 |
| GitNexus CLI/MCP semantic check | direct CLI 当前无法解析 `Anxin-Smart-Legal-Services` repo/cypher；MCP `list_repos` 曾可读；MCP `query/detect_changes` 本次 transport closed | 发布证据必须重新跑 direct CLI 并通过；MCP query/detect 作为工具层风险记录 |
| `gitnexus detect-changes --repo Anxin-Smart-Legal-Services` | 当前未对最新本地提交复跑；历史大 dirty snapshot 为 `380 files / 6037 symbols / 240 affected processes / critical` | 最新本地提交尚未入图；最终 release 前仍需重建/复验 |
| `git status --short` 计数 | 本轮本地交付提交后工作树清洁；后续真实外部证据增量仍必须作为可追踪 release artifact 提交 | `*-local.json` 和 unsigned desktop release support artifact 仍只是支撑证据，不改变 desktop evidence pending 结论 |
| `bash scripts/mypy-baseline-check.sh` | `backend mypy errors: 0 (baseline max: 0)`；`mypy is fully clean` | backend mypy 已清零并由 zero-baseline gate 保护 |
| `bash scripts/release-evidence-secret-scan.sh` | `Release evidence secret scan: PASS` | 新增证据 artifact 未触发明文密钥/PII 扫描失败 |
| `backend/.venv/bin/pytest -q backend/tests/test_commercial_readiness_gate.py backend/tests/test_release_worktree_inventory.py backend/tests/test_release_artifact_validation.py backend/tests/test_release_evidence_secret_scan.py backend/tests/test_release_evidence_validation.py backend/tests/test_commercial_delivery_checklist.py backend/tests/test_commercial_delivery_lanes.py backend/tests/test_sandbox_evidence_runner.py backend/tests/eval/test_rag_full50_runner.py backend/tests/eval/test_rag_quality_smoke.py` | `65 passed` | commercial gate artifact warning propagation、dirty worktree 分类盘点、artifact JSON schema/脱敏边界、RAG full50 provenance、RAG full50 failure diagnostic schema、移动设备手工证据模板 schema、移动/小程序 code smoke schema、桌面 installed-profile smoke schema、桌面 runtime code smoke schema、桌面 release package dry-run schema、桌面 release preflight schema、支付/电签 sandbox preflight/live schema、release evidence secret/PII scan、commercial delivery checklist/lane schema/status/evidence 引用、evidence complete 元数据/表格/artifact/仓库内 artifact 路径存在性/正文冲突/JSON 摘要防假闭环、sandbox env 模板同步/runtime prerequisites、handoff/runbook/48h 计划 live 参数防漂移、live honest-gating validators、RAG full50 preflight 成功/失败诊断保护均有测试覆盖 |
| `bash scripts/commercial-readiness-gate.sh --quick` | quick gate 当前仍 FAIL：Not ready 声明、payment/e-sign/desktop/mobile/agent-governance evidence pending；同时提示 GitNexus store integrity 未检查，需要设置 `GITNEXUS_BIN` 才能做最终 cypher 可读性复验 | gate 仍正确阻止商业发布；企业智能体治理 runtime evidence 现在也由 quick gate 强制检查；GitNexus 不再阻塞日常开发，但仍是最终 release 索引卫生复验项 |
| `bash scripts/commercial-readiness-gate.sh --with-local-tests` | local smoke 已跑过并通过：frontend lint/test/build、frontend Vitest `12 files / 45 tests passed`、workstation settings Playwright `9 passed, 1 skipped`、Agent 审批工作台 Playwright `3 passed, 3 skipped`、desktop cargo check/test `27 passed`、desktop installed-profile SQLCipher/keyring smoke、backend mypy zero baseline、agent governance policy tests `4 passed`、capability route token tests `4 passed`、agent governance model tests `8 passed`、agent governance service tests `6 passed`、agent approval service tests `7 passed`、agent approval API tests `5 passed`、MCP route governance tests `5 passed`、CLI route governance tests `10 passed`、approval authorization guard tests `7 passed`、MCP connection config policy tests `7 passed, 2 deselected`、skill governance gate tests `25 passed`、RAG full50 tests `8 passed`、RAG quality tests `4 passed`、sandbox evidence runner tests `7 passed`、payment/e-sign provider/webhook/refund/action-audit tests `62 passed`、release evidence secret scan tests `2 passed`、release artifact validation tests `30 passed`、commercial readiness gate tests `15 passed`、release worktree inventory tests `2 passed`、release evidence validation tests `9 passed`、commercial checklist tests `5 passed`、commercial lanes tests `3 passed`、sandbox preflight、mobile-device-smoke `8 files / 27 tests passed`、iOS Simulator Expo Go supporting smoke passed；`python3 scripts/validate-release-artifacts.py` 覆盖 `26 JSON artifacts`；最终仍 FAIL：Not ready 声明、payment/e-sign/desktop/mobile/agent-governance evidence pending，并保留最终 GitNexus 索引卫生 warning | 代码级门禁当前通过；外部、签名/真机、企业智能体完整运行时和最终 GitNexus 索引卫生复验证据仍阻断 |
| 支付/电签配置预检 | `payment-sandbox-preflight-20260507.json`、`esign-sandbox-preflight-20260507.json` 写出；live checks not run | 当前缺真实沙箱配置，不能标记 complete |
| RAG full50 内建基线 | built-in preflight、live Qdrant predictions、full metrics 均通过；recall@10 `1.000`、MRR `1.000`、NDCG@10 `0.997` | 内建 RAG baseline 可作为当前 release evidence |

## 3.2 2026-05-08 本轮本地收口证据

本轮没有把商业状态改成 Go，而是把本地能修的“假成功、权限口子、审计缺口、静态演示 UI”收束成可测试的 fail-closed 行为。

| 范围 | 证据 | 当前结论 |
|---|---|---|
| 支付/电签/OA fail-closed | `backend/src/services/payment_service.py`、`backend/src/services/esign_service.py`、`backend/src/services/oa_integration_service.py`；`cd backend && ./.venv/bin/pytest -q tests/test_payment_provider_clients.py tests/test_esign_provider_clients.py tests/test_oa_integration.py ...` -> `95 passed` | staging/production 不再对 mock/未知 provider 静默成功，缺配置会进入明确错误/503 路径 |
| 支付/电签关键动作审计 | `backend/src/api/routes/payments.py`、`backend/src/api/routes/esign.py`、`backend/tests/test_commercial_action_audit.py` | 支付下单、关单、退款和电签发起、取签署链接、取消流程均写入 `AuditLog`；签署 URL 不进入审计内容；真实沙箱阶段需继续校验外部事件与本地审计可对账 |
| 电签组织隔离 | `backend/src/api/routes/esign.py`、`backend/tests/test_external_surface_guards.py` | flow 创建、查询、签署链接和撤销都必须先命中当前用户组织内合同 |
| CLI/MCP 治理 | `backend/src/api/routes/cli.py`、`backend/src/api/routes/mcp_routes.py`、`backend/src/mcp_server.py`、`backend/src/services/mcp_client_service.py`、`backend/tests/test_cli_route.py`、`backend/tests/test_config_commercial_guards.py`、`backend/tests/test_mcp_route_governance.py` | CLI Key 绑定真实用户/scope；key create/revoke/execute 写审计；CLI `/execute` 在 staging/production 默认要求 DB-backed route token，按 API Key `key_id` 绑定 consumer，缺 token 和 consumer mismatch 均 fail-closed；MCP 管理 API 需要 `manage:system`；MCP 管理动作写脱敏审计；独立 MCP 在商业环境默认关闭；外部 MCP 连接已补 stdio command/command-line/env allowlist、SSE scheme/host allowlist 和子进程最小环境；MCP tool execution 在 staging/production 默认要求 DB-backed route token，缺 token、撤销 route 和 consumer mismatch 均 fail-closed |
| Agent capability policy | `backend/src/harness/policy_engine.py`、`backend/src/harness/tool_registry.py`、`backend/src/agents/base.py`、`backend/tests/test_harness.py` | 未注册工具 fail-closed；显式 `allowed_tools` 生效；订阅 feature、角色 permission、绝密模式、设备信任、通道策略和高风险审批上下文可进入统一判定；agent 暴露 MCP tool list 前会过滤，模型返回 tool call 后执行前会二次判权；`tests/test_harness.py -k 'PolicyEngine or AgentMcpToolPolicy'` 为 `13 passed` |
| Approval authorization | `backend/src/api/routes/approvals.py`、`backend/tests/test_business_authorization_guards.py` | 批量审批必须满足当前审批人/组织 scope，审批列表默认按 scope 过滤，`admin` 等同组织管理员而非全局管理员，模板 CRUD/使用按管理员与创建者边界收口，过期审批不能继续执行；`test_business_authorization_guards.py -k 'approval or template'` 为 `7 passed` |
| Skills 进化门禁 | `backend/src/services/skill_evolution_service.py`、`backend/src/services/skill_service.py`、`backend/tests/test_skill_evolution_service.py`、`backend/tests/test_skill_service.py` | Agent 只能创建 draft proposal；required eval checks 缺失或失败会拒绝审批；审批角色限制为 owner/boss/super_admin/org_admin/admin；灰度百分比 1-100；回滚后 governed `SkillService` 立即回到上一版本；`test_skill_evolution_service.py` + `test_skill_service.py` 为 `9 passed` |
| 订阅/JWT 默认安全 | `backend/src/services/subscription_service.py`、`backend/src/core/config.py`、`backend/tests/test_mode_subscription_guards.py`、`backend/tests/test_config_commercial_guards.py` | 未知 feature 默认拒绝；staging 也拒绝默认 JWT secret |
| UI/UX 静态演示去除 | `frontend/src/components/chat/DocumentDiff.tsx`、`AnalysisView.tsx`、`ContextPane.tsx`；`cd frontend && npx eslint ... --max-warnings 0`、`npm test`、`npm run build` | `DocumentDiff` 不再展示静态合同样例，只渲染当前会话真实 diff 数据 |
| 移动端假提交去除 | `mobile/app/(tabs)/investigation.tsx`、`mobile/app/(tabs)/knowledge.tsx`；`cd mobile && npm run typecheck`、`npm test` | 尽调与知识库搜索改为真实 API 调用，并补加载态/错误态 |
| 桌面同步假成功去除 | `desktop/src/services/sync_engine.rs`；`cd desktop && cargo test sync_engine` -> `3 passed` | Rust 云同步数据面未启用时返回 Error 状态，不再 `Ok(0)` 假成功 |
| 桌面绝密模式出站熔断 | `desktop/src/commands/privacy_guard.rs`、`desktop/src/commands/cli.rs`、`desktop/src/commands/sync.rs`、`desktop/src/commands/offline_tasks.rs`、`desktop/src/lib.rs`、`desktop/Cargo.toml`、`desktop/capabilities/default.json`、`desktop/tauri.conf.json`、`frontend/src/lib/api.ts`、`frontend/src/lib/api.test.ts`、`frontend/src/lib/api-adapter.ts`、`frontend/src/lib/api-adapter.sync.test.ts`、`frontend/src/App.tsx`、`frontend/src/lib/store.ts`、`frontend/package.json`、`scripts/desktop-network-surface-gate.sh`；`cd desktop && cargo test` -> `21 passed`；`cd frontend && npm test -- api.test.ts api-adapter.sync.test.ts` -> `17 passed`；`cd frontend && npm run lint`、`npm exec tsc -- --noEmit`、`npm run build` 通过；`bash scripts/desktop-network-surface-gate.sh` -> `PASS` | TopSecret 下桌面 CLI、同步冲突上报、Harness artifact 推/拉、前端本地同步桥、统一 API fetch、WebView 全局 fetch 与 WebSocket 先 fail-closed，不再直接出站；Tauri 原生 HTTP 插件、前端 HTTP 插件包、活跃 `http:*` capability、宽泛 `shell:allow-open` / `opener:*` launch capability、`script-src 'unsafe-eval'` 和 `img-src https://*` 已移除并纳入门禁；signed/notarized packaged runtime 出站拦截和外部脚本运行时复验证据仍需补 |
| 桌面主工作站最小入口 | `frontend/src/components/desktop/DesktopWorkstationPanel.tsx`、`frontend/src/components/desktop/desktopWorkstationModel.ts`、`frontend/src/components/desktop/desktopWorkstationModel.test.ts`、`frontend/src/pages/settingsTabs.ts`、`frontend/src/pages/settingsTabs.test.ts`、`frontend/src/pages/Settings.tsx`、`frontend/src/components/Layout.tsx`、`frontend/e2e/settings-workstation.spec.ts`；`cd frontend && npm test -- desktopWorkstationModel.test.ts settingsTabs.test.ts` -> `6 passed`；`cd frontend && npx playwright test e2e/settings-workstation.spec.ts --project=chromium --project=mobile` -> `9 passed, 1 skipped`；`npm exec tsc -- --noEmit`、`npm run lint`、`npm test`、`npm run build` 通过 | 已补 `/settings?tab=workstation` 可见入口、顶部工作站入口、TopSecret-safe action model、本地 LLM/模型数量/离线队列、知识库数量/文档数、MCP 服务/启用/工具缓存只读探针和移动远控安全闸；绝密模式不启用跨端同步/移动远控等出站动作，且跳过知识库/MCP 数据网络探针；非桌面预览不启用本地模型/同步/远控桌面专属动作，Skills/MCP 只能进入治理视图；浏览器级证据覆盖 direct/legacy route、tab URL 写回、非桌面禁用态、模拟桌面运行时探针和移动宽度不越界；仍缺完整配置 CRUD、桌面 host 真实执行和 signed runtime UI/出站证据 |
| 移动远控控制面 | `backend/src/api/routes/sync.py`、`backend/src/services/remote_control_service.py`、`backend/src/models/sync.py`、`backend/alembic/versions/042_add_remote_control_queue.py`、`backend/alembic/versions/043_add_remote_control_execution_state.py`、`backend/tests/test_remote_control_fail_closed.py`、`desktop/src/services/remote_control_host.rs`、`desktop/src/commands/remote_control.rs`、`mobile/app/desktop-control.tsx`、`mobile/src/services/api.ts`、`mobile/src/features/desktop-control/model.ts`；`cd backend && ./.venv/bin/pytest -q tests/test_remote_control_fail_closed.py` -> `9 passed`；`cd backend && ./.venv/bin/ruff check src/api/routes/sync.py src/models/sync.py src/services/remote_control_service.py tests/test_remote_control_fail_closed.py alembic/versions/043_add_remote_control_execution_state.py` -> `All checks passed`；`cd desktop && cargo test remote_control` -> `6 passed`；`cd mobile && npm test -- desktop-control api.test.ts` -> `2 passed / 17 tests`；`cd mobile && npm test` -> `8 files / 32 tests passed`；`cd mobile && npm run typecheck` -> exit `0` | 已补 DB-backed `/sync/remote-control/status`、`pairings`、`pairings/{id}/confirm`、`route-token`、`commands`、`commands/claim`、`commands/{id}/status`、`commands/{id}/cancel`、`audit-events` 和 `commands/{id}`：本地/绝密模式拒绝；配对需桌面确认；命令需短期 CapabilityRoute token、consumer/scope 校验、二次确认和未过期配对；桌面 host 可领取 queued 命令并回传 running/completed/failed；queued/claimed 命令可取消；队列 payload、执行结果和审计 metadata 会递归脱敏 token/secret 字段。桌面端已补 Tauri IPC host 客户端，用登录态 bearer token 调用 confirm/claim/status API，并由 TopSecret data-network guard、本地 payload/状态校验和单测锁住；桌面端还新增显式单次 safe-probe host cycle：只完成 `ping/status_probe`，未知命令统一 failed/unsupported。移动端已补 `/desktop-control` 状态页与个人中心入口，local 模式不发网络请求，hybrid/cloud 读取后端安全闸；当后端返回 confirmed pairing 的 `desktop_device_id` 与 `pairing_id` 时，移动端可申请短期 route token 并只入队 `desktop.status_probe`，UI 明确显示只是入队等待桌面 host 回传，不伪造执行成功。仍缺常驻后台自动轮询、高风险真实执行器、真实跨设备联调、signed runtime 和真机证据 |
| 发布证据链 | `docs/release/test-evidence.md`、`docs/release/completion-audit.md`、`docs/release/commercial-delivery-readiness.md`、`docs/release/security-and-privacy-checklist.md`、`docs/release/commercial-delivery-checklist.json` | 本轮新增代码级证据已同步进 release 文档，仍保留 payment/e-sign/desktop/mobile/agent-governance pending |

## 4. 仍阻断商业完成的要求

1. 支付真实渠道未闭环：`docs/release/evidence/payment-sandbox.md` 仍为 pending；`scripts/sandbox-evidence-runner.py` 已补预检/live 采集入口并写出 `payment-sandbox-preflight-20260507.json`，但当前缺 WeChat Pay/Alipay app/merchant/key/callback/webhook 配置，且仍缺真实沙箱成功/失败/退款/关闭/证书轮换/异步通知重试证据。
2. 电签真实渠道未闭环：`docs/release/evidence/esign-sandbox.md` 仍为 pending；`scripts/sandbox-evidence-runner.py` 已补预检/live 采集入口并写出 `esign-sandbox-preflight-20260507.json`，但当前缺 e签宝/法大大 app ID、app secret、官方 webhook enable 配置，且仍缺真实签署、撤销、下载、回调事件、灰度记录。
3. 桌面商业安全未闭环：`docs/release/evidence/desktop-runtime-smoke.md` 仍为 pending；SQLCipher/keyring 安全门禁、本地 installed-profile keyring/reopen 与 plaintext migration smoke、debug binary self-test、SQLite 100/500 本地性能基线、production APNs entitlement、release package dry-run、signed/notarized release preflight、unsigned release app+DMG build、unsigned release packaged runtime smoke 和 unsigned release packaged-profile/performance smoke 已补；fresh debug `.app` runtime 复跑当前在 AppKit registration 前 abort；preflight 明确缺 Tauri signingIdentity、Apple codesign identity、notary credentials、codesign verify、signature authority，且 signed/notarized installer、signed packaged-profile migration transcript、跨端连续会话、signed packaged runtime 性能基线仍缺失。
4. 移动/小程序真机验收缺失：`docs/release/evidence/mobile-device-smoke.md` 仍为 pending；本地 `scripts/mobile-device-smoke.sh --out docs/release/evidence/artifacts/mobile-mini-code-smoke-20260508.json --manual-template-out docs/release/evidence/artifacts/mobile-device-manual-template-20260508.json` 已通过并覆盖移动测试、tsc、mobile result surface guard、mobile lawyer conversion guard、Expo config/SDK/Metro config guard、Expo doctor、mobile npm audit、小程序 tsc/build、WeChat DevTools CLI project smoke、refresh auth guard、fake fallback guard、mini-program navigation boundary guard 与 mini-program design token guard，且写出代码级 artifact 和手工设备证据模板；`mobile-ios-simulator-expo-go-smoke-20260508.json` 已证明 iOS Simulator + Expo Go 本地 bundle，但它只是支持性证据并保持 `release_evidence_complete=false`；`mobile-device-host-probe-20260508.json` 确认 iOS Simulator 可用，但真实/官方 iOS 设备故事、Android、交互式微信开发者工具关键路径手测和跨设备连续会话记录未完成。
5. mypy 已清零：当前 backend mypy `0` 个错误，`scripts/mypy-baseline-check.sh` 默认 ceiling 已降为 `0`；后续任何非零 mypy 都应阻断 release readiness。
6. 全设备智能助手未闭环：桌面主工作站最小入口、TopSecret-safe action model、Settings 工作站浏览器级路由/移动宽度证据、本地 LLM/模型数量/离线队列/知识库/MCP 只读探针、移动远控 DB-backed 配对/授权/队列/host 领取/状态回传/取消/审计控制面、桌面 Tauri IPC host 客户端、单次 safe-probe host cycle 以及移动端 route-token + `desktop.status_probe` 入队面已补；但完整配置 CRUD、常驻后台自动轮询、高风险真实执行器、真实移动远控跨设备联调、任意 LLM/Skills/MCP 的组织级策略、独立本地知识库体验和绝密模式 signed/notarized packaged runtime 出站证据仍缺失；当前只能称为已有基础，不能称为完成态。桌面 CLI/sync/Harness IPC、本地同步桥、统一 API fetch、WebView 全局 fetch、WebSocket、Tauri/前端 HTTP 插件面和宽松 CSP 已先补代码级 TopSecret/network-surface guard。
7. 企业智能体治理仍是局部硬化：CLI/MCP 已有用户绑定、系统权限、审计防线和 DB-backed route-token 执行边界，后端 `/cli/route-token` 与桌面端 execute 前 route-token 获取/传递已补，外部 MCP 连接已补商业环境 allowlist 基线，MCP tool execution 已接入 DB-backed route token，agent capability policy 已补未注册工具 fail-closed、上下文判权、MCP runtime 二次判权、Harness 可用工具列表上下文过滤、短期 route-token broker，以及 AgentManager/Team/Worker/Human/ChannelPolicy/CapabilityRoute/TokenLease/Approval/AuditEvent 持久化模型与迁移；模型层已补复合租户外键、TokenLease hash-only 和审计 update/delete 防线；AgentApprovalService 与 `/agent-approvals` API 已补高风险审批创建、payload 脱敏、组织/本人 scope、授权角色审批/驳回、撤销、执行前 validate、过期/撤销 fail-closed、action/route 匹配、scoped audit-events、scoped audit-export、workspace-control 运行时未接入 fail-closed 和审计回归；SkillGovernance 已补 DB-backed proposal/eval/approval/gray/rollback/enabled-version/audit 持久化、组织隔离、正式 API、普通员工只能提案、管理员评测/审批/发布/回滚和审计导出回归；前端最小 Agent 治理工作台已补桌面入口、移动协作导航、能力策略可用/阻断工具展示、审批状态筛选、payload 预览、审批审计时间线、审批审计 JSON 导出、批准/驳回/撤销动作、已批准行控制拒绝提示，以及 Skill 提案创建、评测、批准、灰度、审计时间线和审计导出；但真实 approved MCP connector 演练、LLM/browser/desktop-control route-token 接入、CapabilityRoute/TokenLease/AgentApproval/AgentAuditEvent/SkillGovernance 全运行时集成、完整 Human-in-the-loop 旁听、真实暂停/接管/终止执行效果、artifact 工作室、记忆治理和跨进程撤销失权仍未实现为完整闭环。

## 5. 完成判定

当前目标不能标记完成。下一步应按阻断优先级继续：

1. 在可用沙箱账号下跑支付/电签真实渠道验收。
2. 补桌面签名/公证输入后生成 signed/notarized installer，再复跑 signed packaged UI interaction、signed packaged-profile migration transcript、signed packaged runtime 性能基线与跨端连续会话证据。
3. 补移动/小程序真机验收，并保持 Ruff/mypy 零回退。
4. 补桌面主工作站、移动远控、任意 LLM/Skills/MCP、独立知识库和绝密模式出站拦截的可复跑证据。
5. 开发前继续使用 `GITNEXUS_BIN` direct rc CLI 做影响分析，避免 `npx gitnexus@rc` wrapper bug。
