# AI法务智能体系统 - 前端开发 Dockerfile
# 支持 HMR 热更新，修改代码后浏览器自动刷新

FROM node:20-alpine

WORKDIR /app

# 安装依赖（利用 Docker 缓存层）
COPY package*.json ./
RUN --mount=type=cache,target=/root/.npm \
    npm ci

# 源代码通过 volume 挂载，无需 COPY
EXPOSE 3001

CMD ["npx", "vite", "--port", "3001", "--host", "0.0.0.0"]
