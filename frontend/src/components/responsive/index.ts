/**
 * Responsive Primitives — 响应式原子组件
 *
 * 目标：让业务代码不再直接写断点判断，响应式行为封装在原语层。
 *
 * 配套 hooks：useBreakpoint, useMediaQuery（位于 @/hooks）
 */

export { ScrollShell } from './ScrollShell'
export { SplitView } from './SplitView'
